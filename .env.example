# Optional. Without this the game runs fully in memory (great for local dev),
# but a server restart clears any game in progress.
# Set REDIS_URL (e.g. a free Upstash Redis database) to make live games
# survive restarts and deploys.
# REDIS_URL=rediss://default:<token>@<host>.upstash.io:6379
